export default class User{
  constructor(usuario, password, nombre, perfil, foto, estado, id){
    this.usuario = usuario
    this.password = password
    this.nombre = nombre
    this.perfil = perfil
    this.foto = foto
    this.estado = estado
    this.id = id
  }
}